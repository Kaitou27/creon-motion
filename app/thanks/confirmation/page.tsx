import ConfirmationThanks from '../../../Components/typage/ConfirmationThanks';

export const metadata = {
  title: 'Booking Confirmed | Creon Motion',
  description: 'Meeting successfully confirmed and scheduled.',
};

export default function ConfirmationPage() {
  return <ConfirmationThanks />;
}
